__del__(self)



对象即将销毁时，就会自动调用此方法
def __del__(self):
	print("提示")
作用：

1 如果想要在对象销毁/删除/释放前做某些事件
2 通过此方是否调用就可以知道对象是否正常释放


# 对象即将销毁时,就会自动调用此方法
 """如果想要在对象释放前做某些事件,临终遗言,可以在此方法中写
通过查看此方法是否调用来判定对象是否能正常释放
如果对象不能正常释放,就会导致内存泄漏"""